services:
  - type: web
    name: pdftools-backend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    plan: free
    region: oregon  # or singapore for better latency
    envVars:
      - key: PORT
        value: 10000
      - key: FLASK_ENV
        value: production
      - key: PYTHONUNBUFFERED
        value: 1
      - key: ALLOWED_ORIGINS
        value: "*"
      - key: MAX_CONTENT_LENGTH
        value: "104857600"
      - key: GUNICORN_CMD_ARGS
        value: "--timeout 600 --workers 1 --threads 1 --worker-class sync --preload"
    healthCheckPath: /health
    autoDeploy: true
    buildCommand: echo "Building with Docker..."
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 300 --worker-class sync --max-requests 100 --max-requests-jitter 10 --access-logfile - --error-logfile - --log-level debug --graceful-timeout 120 --keep-alive 5
    # Increase timeouts for free tier cold starts
    initialDelaySeconds: 90
