# Fast Excel/CSV to PDF Converter - Optimized for Cloud Deployments
# Minimal dependencies for faster installation on Render/Vercel

# Core dependencies
Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1

# Production server
gunicorn==21.2.0

# Excel/CSV processing (lightweight)
openpyxl==3.1.2
pandas==2.1.4

# PDF generation (pure Python)
reportlab==4.0.7

# PDF to Excel conversion (fast)
tabula-py==2.9.0
PyMuPDF==1.23.8

# Optional: for better performance
numpy==1.26.2

# Image processing (for reportlab)
Pillow==10.1.0

# Note: For full features including camelot, use requirements.txt instead
# This file is optimized for speed and minimal dependencies
